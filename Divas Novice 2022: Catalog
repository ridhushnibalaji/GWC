n = int(input())
IDs = []
for i in range(n):
    ID = input()
    sum = 0
    for j in ID:
        if j.isdigit():
            sum += int(j)
    IDs.append([sum, ID])
IDs.sort()

for i in IDs:
    print("{} {}".format(i[1], i[0]))
